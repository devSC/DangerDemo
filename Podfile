platform :ios, "8.0"
inhibit_all_warnings! # this will disable all the warnings for all pods

target "DangerDemo" do
    pod "AFNetworking",         "3.1.0"
    pod "ReactiveCocoa",        "2.5"

end
